import icons from './icons'
import rounded from './rounded'
import sizes from './sizes'

export default {
  ...icons,
  ...rounded,
  ...sizes,
  wrapper: 'mt-2.5 flex items-center justify-start gap-1.5',
  icon: 'flex flex-center bg-primary-50 text-primary',
}
